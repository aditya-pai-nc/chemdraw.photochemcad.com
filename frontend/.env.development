# `next dev` runs on :3000 and talks to uvicorn on :8000 directly.
# FastAPI already sends permissive CORS headers, so no dev proxy is needed.
# Leave this unset in production — the export is served by FastAPI itself,
# so /api is same-origin.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
